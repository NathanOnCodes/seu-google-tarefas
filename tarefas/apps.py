from django.apps import AppConfig


class TarefasConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'tarefas'
    verbose_name = "Area de tarefas"
    verbose_name_plural = "Areas de tarefas"
